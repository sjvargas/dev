<h1><%= @post.title %></h1>
<div><%= @post.body %></div>
<%if @post.body.length >150%>
<div> (post completo)</div>
<%else%>
<div>-</div>
<%end%>

<div><%=link_to "Edit post", edit_post_path(@post)%></div>
<div><%=link_to "Delete post", post_path(@post), method: :delete, data: {confirm: "Are you sure?"}   %></div>